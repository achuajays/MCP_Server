# Example Anime MCP

A simple example of an MCP server that returns anime titles.

## How to use

This MCP server is configured to return anime titles. You can query it by sending a GET request to the root URL of the server with the query parameter `q` set to the title of the anime you're looking for.

For example, to query the server for the title "Attack on Titan", you can use the following URL:


![Example Image](image.png)



    